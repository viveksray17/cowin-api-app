<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">
    {% if title %}
      <title>Cowin App - {{ title }}</title>
    {% else %}
      <title>Cowin App</title>
    {% endif %}
  </head>
  <body>
    {% block content %}
    {% endblock content %}
  </body>
</html>
